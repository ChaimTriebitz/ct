import { svgs } from './svgs'

export const svgMap = {

   stack: {
      nodejs: svgs.nodejs,
      scss: svgs.scss,
      css: svgs.css,
      html: svgs.html,
      js: svgs.js,
      reactjs: svgs.reactjs,
      express: svgs.expressjs,
      mongodb: svgs.mongodb,
      nextjs: svgs.nextjs,
   }
}