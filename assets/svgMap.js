import { svgs } from './svgs'

export const svgMap = {
   stack: {
      nodejs: svgs.nodejs,
      scss: svgs.scss,
      css: svgs.css,
      html: svgs.html,
      js: svgs.js,
      reactjs: svgs.reactjs,
      express: svgs.expressjs,
      mongodb: svgs.mongodb,
      sql: svgs.sql,
      mysql: svgs.mysql,

      // postgresql: svgs,
      // redis: svgs,
      // firebase: svgs,
      // graphql: svgs,
      // typescript: svgs,
      // nextjs: svgs,
      // tailwindcss: svgs,
      // bootstrap: svgs,
      // bulma: svgs,
      // materialize: svgs,
      // foundation: svgs,
      // semanticui: svgs,
      // jquery: svgs,
      // sass: svgs,
      // less: svgs,
      // stylus: svgs,

      // webpack: svgs,
      // gulp: svgs,
      // grunt: svgs,
      // rollup: svgs,
      // vite: svgs,
      // parcel: svgs,
      // esbuild: svgs,
      // snowpack: svgs,
      // browserify: svgs,

      // pm2: svgs,
      // forever: svgs,
      // nodemon: svgs,
      // pm2: svgs,
      // forever: svgs,
      // nodemon: svgs,
      // docker: svgs,
      // kubernetes: svgs,
      // aws: svgs,
      // azure: svgs,
      // googlecloud: svgs,
      // heroku: svgs,
      // netlify: svgs,
      // vercel: svgs,
      // firebase: svgs,
      // cloudflare: svgs,
      // digitalocean: svgs,
      // linode: svgs,
      // vultr: svgs,
      // rackspace: svgs,
      // linode: svgs,
      // vultr: svgs,
      // rackspace: svgs,
      // heroku: svgs,
      // netlify: svgs,
      // vercel: svgs,
      // firebase: svgs,
      // cloudflare: svgs,

   }
}